# Example .env - DO NOT commit real credentials
# In production / GitHub Actions use the repository secret named DATABASE_URI
DATABASE_URI=postgres://username:password@host:5432/database_name
PORT=3000
NODE_ENV=development